# ──────────────────────────────────────────────
# 仓库说明
#
# 这个仓库存放 Hexo 博客的【源码】，即：
#   - Markdown 文章（source/_posts/）
#   - 配置文件（_config.yml、_config.butterfly.yml）
#   - 自定义 JS/CSS（source/js/、source/css/）
#   - GitHub Actions 工作流（.github/workflows/）
#
# 以下文件【不】进入仓库，原因各见注释。
# ──────────────────────────────────────────────

# 操作系统产生的垃圾文件，与项目无关
.DS_Store
Thumbs.db

# Hexo 运行时数据库，记录文件修改时间用于增量构建，
# 每台机器各自生成，提交会引起无意义 diff
db.json

# 所有日志文件，运行时产生，内容因环境而异
*.log

# npm 依赖，体积庞大（数百 MB），通过 package.json 描述，
# 构建时由 npm ci 自动安装，无需纳入版本控制
node_modules/

# Hexo 构建产物（静态 HTML/CSS/JS），
# 由 GitHub Actions 自动生成并推送到 gh-pages 分支，
# 源码仓库只需保存源文件
public/

# hexo-deployer-git 的本地部署缓存目录，
# 本项目已改用 GitHub Actions 部署，该目录不会产生，保留为预防
.deploy*/

# 多配置合并文件，hexo 命令行临时生成，不属于源码
_multiconfig.yml

# Butterfly 主题本身是第三方仓库，
# 由 GitHub Actions 构建时 git clone 安装，
# 不提交进来避免仓库臃肿（主题约 50MB）
themes/butterfly/

# Hexo 构建缓存和临时文件，本地运行产生，与源码无关
.cache/
*.tmp

# VS Code 工作区配置，属于个人编辑器偏好，不应强加给其他贡献者
.vscode/

# Vim/其他编辑器的交换文件，编辑时产生，无内容价值
*.swp
*.swo

# Claude Code 的本地工作数据（计划文件、记忆文件、会话缓存），
# 属于 AI 辅助开发的本地状态，不属于博客源码
.claude/

# 系统 crontab 触发 Claude 迭代任务时写入的日志，
# 运行时产生，内容因机器和时间而异
cron.log
